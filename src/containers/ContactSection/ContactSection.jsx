import "./ContactSection.css";

const ContactSection = () => {
    return (
        <section id="contact">
            <p>Contact Section</p>
        </section>
    );
};

export default ContactSection;
